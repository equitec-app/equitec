console.log("Entro al update.js");
<%if @headquarter.errors.any?%>
  console.log("tiene errores");
  $('.modals').html("<%= escape_javascript render(:partial => 'customers/edit_headquarter_form') %>");
  $('#editHeadquarterModal').modal('show')
<%else%>
  console.log("no tiene errores");
  $('.modals').empty();
  $('.customer-profile-container').html("<%= escape_javascript render(:partial => 'customers/customer_profile') %>");
  $('.modal-backdrop').remove();
  $('body').removeClass('modal-open')
<%end%>
